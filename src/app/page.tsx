import React from "react";

import Home from "@/app/home";

export default function Page() {
  // TODO: We should query for user here and redirect to the dashboard earlier if required
  return <Home />;
}
